import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    /* flex: 1, */
    flexDirection: "row",
    gap: 10,
  },

  main:{
    flex:1,
    marginTop: 30,
    marginLeft: 10,
    marginRight: 10,
  }
});
