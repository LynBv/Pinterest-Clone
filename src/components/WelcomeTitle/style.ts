import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  avatarImage: {
    width: 80,
    height: 80,
    margin: "auto"
  },

  welcomeBox:{
    marginTop: "40%",
    justifyContent:"center",
    marginBottom: 15,
  },

  text:{
    marginTop: 10,
    fontWeight: 'bold',
    fontSize: 25,
    textAlign: "center"
  }
});