import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  avatarImage: {
    width: 100,
    height: 100,
    margin: "auto"
  },

  welcomeBox:{
    marginTop: "40%",
    justifyContent:"center",
    marginBottom: 15,
  },

  text:{
    marginTop: 10,
    fontWeight: 'bold',
    fontSize: 30,
    textAlign: "center"
  }
});